[project]
name = "study-tracker-frontend"
version = "0.1.0"
description = "Python frontend for the Pomodoro API"
readme = "README.md"
authors = [
    { name = "Jason Standen", email = "jason@merl.one" }
]
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.37.0",
]
